import org.gradle.api.initialization.resolve.RepositoriesMode

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // i changed to prefer settings, it was different
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "i190417_i192048"
include ':app'
